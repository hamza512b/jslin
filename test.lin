DEF FIZ_BUZ n
    IF n % 3 == 0 and n % 5 == 0
        RETURN "FIZBUZ"

    IF n % 3 == 0
        RETURN "FIZ"
    
    IF n % 5 == 0
        RETURN "BUZ"

    RETURN none

INIT i 1
WHILE i < 4
    PRINT (FIZ_BUZ i)
    SET i i + 1